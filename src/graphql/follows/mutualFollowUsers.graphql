query MutualFollowUsers($first: Int, $after: String) {
  followers(first: $first, after: $after) {
    edges {
      node {
        id
        name
        profileText
        avatarUrl
        isFollowing
        followingCount
        followersCount
        createdAt
        updatedAt
      }
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
